# 使用 product 模块数据库（product_manage）
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ec_system?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: zjd056088
