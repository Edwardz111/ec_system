<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ec.mapper.OrderItemMapper">
    <insert id="insert" parameterType="com.ec.pojo.OrderItem">
        INSERT INTO order_items (order_id, product_id, quantity, price, created_at)
        VALUES (#{orderId}, #{productId}, #{quantity}, #{price}, NOW())
    </insert>

    <insert id="insertBatch">
        INSERT INTO order_items (order_id, product_id, quantity, price, created_at)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.quantity}, #{item.price}, NOW())
        </foreach>
    </insert>
    
    <select id="findByOrderId" parameterType="java.lang.Long" resultType="com.ec.pojo.OrderItem">
        SELECT id, order_id as orderId, product_id as productId, quantity, price, created_at as createTime
        FROM order_items WHERE order_id = #{orderId}
    </select>
    
    <delete id="deleteByOrderId" parameterType="java.lang.Long">
        DELETE FROM order_items WHERE order_id = #{orderId}
    </delete>
</mapper>